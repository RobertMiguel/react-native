import { StyleSheet } from "react-native";

const createStyles = () =>
  StyleSheet.create({
    container: {
      height: "50%",
      width: "100%",
      justifyContent: "center",
      alignItems: "center",
    },
  });

export default createStyles;
